# @package _global_

# to execute this experiment run:
# python train.py experiment=flaml_suicide

defaults:
  - override /data: health
  - override /model: flaml
  - override /callbacks: [classification_metrics,confusion_matrix]
  - override /trainer: cpu

tags: ["health", "flaml"]

seed: 12345

trainer:
  min_epochs: 1
  max_epochs: 1

data:
  batch_size: 16
